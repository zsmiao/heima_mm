<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--加载外部配置文件-->
    <properties resource="jdbc.properties"/>
    <!--配置类型别名-->
    <settings>
        <!--mybatis日志-->
        <setting name="logImpl" value="STDOUT_LOGGING"/>
        <!--全局延迟加载配置-->
        <setting name="lazyLoadingEnabled" value="true"/>
        <!--开启自动驼峰命名规则-->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>
    <typeAliases>
        <package name="xyz.itclay.heima_mm.domain"/>
    </typeAliases>
    <!--    配置PageHelper插件-->
    <plugins>
        <plugin interceptor="com.github.pagehelper.PageInterceptor">
            <!--指定使用的数据库-->
            <property name="dialectHelper" value="mysql"/>
        </plugin>
    </plugins>
    <!--    环境配置-->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <!--type="POOLED" 自定义druid数据源-->
            <dataSource type="xyz.itclay.heima_mm.utils.DruidDataSourceUtils">

                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>

                <!--   <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                   <property name="url" value="jdbc:mysql://www.mysql.com:3306/usermanager"/>
                   <property name="username" value="root"/>
                   <property name="password" value="123456"/>-->

            </dataSource>
        </environment>
    </environments>
    <!--    加载映射配置-->
    <mappers>
        <package name="xyz.itclay.heima_mm.mapper"/>
    </mappers>


</configuration>